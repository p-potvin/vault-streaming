# Vault Streaming — Work Plan / Resume Point — Wed, 22 Jul 2026 09:10

Context: quota may cut off mid-session. This file is the resume point.

## ✅ Done this session

- Fixed **settings not saving** — dead `settings-default-folder` ref threw before `saveSettings`. `js/settings/core.js`
- Fixed **Library tab crash** — removed toolbar IDs (`search-box`/`filter-type`/`sort-by`/`btn-sort-order`) guarded. `js/favorites.js`
- **Library movie click auto-plays** best cached source (skips details modal); series open to season screen. `js/favorites.js`
- **Series remembers last-watched season** in the picker. `js/streaming/details-modal.js`
- **Poster fix** — direct movie play passes movie's own poster/year to `triggerRDStream(...,meta)` (no stale watch-history thumbnail). `js/streaming/rd-flow.js` + `js/favorites.js`
- Ticked completed items in `TODO.md`.

---

## ⛔ Plan-only (blocked on runtime / infra / quota)

### A. TMDB Discover "Unknown Error" at boot

- On-screen message is literally **"Unknown error"** → in `js/tmdb.js:175` that string only appears when `response` itself is **null/undefined** (not `response.error`). So the `discover-tmdb` IPC returned nothing OR `renderTMDB` threw before/around the invoke.
- Ruled out: token invalid (verified HTTP 200), API unreachable, `.env` not loading (loader paths OK).
- Prime suspects (check in order):
  1. **Boot-order / provider state race** — `window.tmdbCurrentProvider` / `tmdbCurrentMediaType` undefined when `renderTMDB` fires (EXTRACTION-NOTES warns discover must render *after* `initTMDBListeners`). If a renderer-side throw happens before `electronAPI.discoverTMDB(...)`, `response` stays undefined → "Unknown error".
  2. **Bad provider id** in the discover URL (`with_watch_providers`) — but that yields empty results ("No results"), not a null response, so lower priority.
- Next actions when back:
  1. Launch app, open DevTools console. Look for `[TMDB] Discovering streaming items: <url>` (main process).
     - **Absent** → renderer threw before invoke (fix boot order / guard provider state).
     - **Present + error** → inspect the fetched URL / HTTP status.
  2. Log `window.tmdbCurrentProvider`, `tmdbCurrentMediaType`, `tmdbCurrentPage` at boot.

### B. Comet "Provider is currently unavailable" (RD stream)

- Playback dialog resolves then player shows "Provider is currently unavailable".
- Working theory (user): RD key was rotated; **Comet may hold a stale RD key baked into its config** on ovhcloud (`100.67.25.118:5173`).
- Policy: all debrid/torrent must route through Comet; **do NOT call Real-Debrid directly**. Fix is Comet-side, not app-side.
- Plan (no request batches — request-safety policy; one diagnostic check max, with approval):
  1. Confirm app `.env` `REAL_DEBRID_API_TOKEN` == the new key.
  2. Determine whether the app passes the RD key to Comet per-request (`COMET_STREAM_BASE`) or relies on Comet's own stored key.
  3. If Comet stores its own RD key → **user updates Comet config on ovhcloud + restarts Comet** (infra action, user-run).
- Likely root cause: RD returns 401 to Comet due to old token → surfaced as "Provider is currently unavailable".

### C. Emoji → SVG replacement  (icon set FOUND: `js/icons.js`, 40 icons)

Usage: `window.icons.<name>('classes','style')` → inline SVG string.
~43 emoji across 9 files (index.html 20, `js/player/clip.js` 11, details-modal 4, others).

**Covered by icons.js — safe to replace:**

| emoji | count | → icon |
|---|---|---|
| ✕ | 12 | `close` |
| ⚡ | 7 | `lightning` |
| ★ | 5 | `star` |
| 🔊 | 1 | `volume` |
| 🔄 | 1 | `refresh` |
| 📥 | 1 | `downloadPlayer` |
| ✓ | 1 | `checkmark` |
| ✂️ | 1 | `crop` |
| ⏸ | 1 | `pause` |
| ✨ | 1 | `magic` (reuse) |

**MISSING from icons.js — need you to supply, or I add new SVGs:**

- `→` arrow-right (only `arrowUp`/`arrowDown` exist)
- `⏮` skip-back / previous
- `⏭` skip-forward / next
- `⏯` play-pause toggle
- `⚙️` settings / gear
- `🎨` palette / theme
- `🗗` window restore (titlebar control)
- `🟪` decorative legend marker (decide: keep or icon)
- `🇦 🇧 🇨 🇫 🇬 🇷` country flags for language/region selector — flags aren't line-icon glyphs; keep emoji or use a small flag-SVG set (your call)

**Recommendation:** supply 6 line icons — arrow-right, skip-back, skip-forward, play-pause, gear, palette. Flags + window control: decide keep-vs-replace.

### D. String extraction (i18n)

- Existing system: `js/translations.en.js` (`window.translationsEn`) + `translations.qc.js` + `translations.js` aggregator; access via `window.translations[lang][key]`. (Note: many legacy vault-explorer keys still present — candidate cleanup.)
- Plan (file-by-file, active surfaces first):
  1. Grep hardcoded user-facing English literals (innerHTML/textContent/`showToast`/`title=`) in `index.html` + `js/**`.
  2. Add keys to `translations.en.js` + `translations.qc.js`.
  3. Replace literals with `t.<key>` (existing pattern `const t = window.translations[window.currentLang==='fr'?'fr':'en']`).
  4. Priority order: `streaming/details-modal.js`, `streaming/rd-flow.js`, `favorites.js`, `tmdb.js`, `player/player.js`, then `index.html`.

---

## 🔎 Verification finding to reconcile

`EXTRACTION-NOTES.md` (Sat 12 Jul trim) lists **"AI live subtitles"** among features **REMOVED** from vault-streaming. But `ROADMAP.md §3` still marks Live Subtitles `[x]`.
→ Current app has **subtitle downloads (OpenSubtitles)** but **not** the live Parakeet transcription (that lives in `vaultwares-realtime/`). ROADMAP §3 needs reconciling — left unchanged pending your call on whether live subs are meant to return here.
